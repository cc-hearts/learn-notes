import { check } from '../src/router'



describe('match success return {course: xxx, lecture: xxx} when match  router is /test/:course/:lecture', () => {
  const path = '/test/123/345'

  // const res = check(path)


})